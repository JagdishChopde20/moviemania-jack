import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hijack.moviediscover',
  appName: 'moviemania',
  webDir: 'dist/moviemania',
  bundledWebRuntime: false
};

export default config;
